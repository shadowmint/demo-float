/// <reference path="__init__.ts"/>
module cherub {
    export module geom {

        /* Mappable points */
        export interface Point {
            x:number;
            y:number;
        }
    }
}
